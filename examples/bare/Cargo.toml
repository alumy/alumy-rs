[package]
name = "alumy-example-bare"
version.workspace = true
edition.workspace = true
publish = false
rust-version.workspace = true

[dependencies]
alumy = { path = "../..", default-features = false, features = ["bare"] }

[target.'cfg(target_arch = "arm")'.dependencies]
cortex-m = "=0.7.7"
cortex-m-rt = "=0.7.3"
panic-halt = "=0.2.0"
